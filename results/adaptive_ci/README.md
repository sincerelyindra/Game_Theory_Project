# Adaptive CI results

Store raw output bundles for the adaptive confidence-interval extension here.

Expected raw archive:
- `raw/part_c_quick.zip`

This folder corresponds to the extension study that compares fixed confidence levels with adaptive rules.
