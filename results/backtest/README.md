# Rolling backtest results

Store raw output bundles for the historical backtest here.

Expected raw archive:
- `raw/tuna_backtest_results(1).zip`

This folder corresponds to the Dominick's scanner-style rolling evaluation reported in the write-up.
